import React, { Component } from "react";

export default class App extends Component {
  render() {
    return (
      // Change the below and you will have a different 'heading',
      //  Whenever we make change file, we just save it, refresh the browser and we will see the new content pop up, thanks to the transpiler webpack
      <div>React simple starter</div>
    );
  }
}
